# ShieldAI Environment Variables
# Copy this file to .env and fill in your values

# ============================================================================
# Anthropic API Configuration (Required for AI features)
# ============================================================================

# Your Anthropic API key for Claude Opus 4.6
# Get it from: https://console.anthropic.com/settings/keys
# Required in production, optional in development (shows warning if missing)
# Store your actual key in ~/.anthropic_api_key (not in this file!)
ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}

# ============================================================================
# Server Configuration
# ============================================================================

# Server port (default: 8484)
PORT=8484

# Host address to bind to (default: 0.0.0.0)
# Use 127.0.0.1 to only allow local connections
HOST=0.0.0.0

# Node environment (development | production | test)
NODE_ENV=development

# Logging level (debug | info | warn | error)
LOG_LEVEL=info

# ============================================================================
# Docker Configuration
# ============================================================================

# Path to Docker socket (default: /var/run/docker.sock)
# For remote Docker: tcp://hostname:2375
DOCKER_SOCKET=/var/run/docker.sock

# ============================================================================
# Authentication (Optional)
# ============================================================================

# Enable bearer token authentication (default: false)
# Set to 'true' to require Authorization header
AUTH_ENABLED=false

# Bearer token for authentication (required if AUTH_ENABLED=true)
# Generate with: openssl rand -base64 32
AUTH_TOKEN=

# ============================================================================
# Advanced Configuration (Usually not needed)
# ============================================================================

# Maximum request body size in bytes (default: 10MB)
# BODY_LIMIT=10485760

# Timeout for Docker operations in milliseconds (default: 30000)
# DOCKER_TIMEOUT=30000

# Maximum number of containers to analyze in one audit (default: 100)
# MAX_CONTAINERS=100

# Enable CORS for specific origin (default: allow all in dev)
# CORS_ORIGIN=https://your-frontend.com

# ============================================================================
# Notes
# ============================================================================

# Startup warnings if missing:
#   - ANTHROPIC_API_KEY (in development) - AI features disabled
#
# Startup errors if missing:
#   - ANTHROPIC_API_KEY (in production) - Server won't start
#   - AUTH_TOKEN (if AUTH_ENABLED=true) - Server won't start
#
# For local development, you only need:
#   - ANTHROPIC_API_KEY (for AI analysis)
#   - Everything else has sensible defaults
#
# For production deployment, you need:
#   - ANTHROPIC_API_KEY (mandatory)
#   - Consider AUTH_ENABLED=true with a strong AUTH_TOKEN
#   - Set NODE_ENV=production
